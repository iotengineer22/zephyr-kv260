# KV260 GPIO Input/Output Test

## Overview

This project includes a program to test GPIO input and output operations on the Kria KV260 Vision AI Starter Kit. It reads the hardware overlay configurations to evaluate and verify the GPIO functionalities.

## Building and Running

To build this application, use the standard Zephyr `west` command with your target board:

`west build -p -b kv260_r5`